import React, { memo } from 'react';
import './TaxIdentification.scss';

export const TaxIdentification = memo(function TaxIdentification() {
  return <div className="tax-identification">
    TaxIdentification
  </div>;
});
